# AI 协作约定

本文件适用于整个 Personal Blog 项目。任何 AI 或开发者修改项目内容时都必须遵守以下规则。

## 开始工作前

1. 阅读根目录 `README.md`。
2. 阅读 `ai/README.md`，确认文档版本与迭代规则。
3. 阅读当前迭代的 `requirements.md`、`architecture-design.md`、`tasks.md` 和 `test-plan.md`。
4. 检查工作区现有变更，不覆盖或回退与当前任务无关的内容。

## 需求与设计

- `requirements.md` 是当前迭代的产品需求基线，代码行为不得与其冲突。
- 技术实现必须遵守 `architecture-design.md` 中的模块边界。
- 需求或架构发生实质变化时，先更新对应文档和 `decisions.md`，再修改代码。
- 历史迭代只读；新需求使用新的 `NNN-short-name` 迭代目录记录。
- 未经明确决策，不得引入微服务、消息队列、分布式事务或新的基础设施。

## 数据和权限红线

- 所有用户数据都必须具有明确的所有者或平台归属。
- 服务端必须依据当前认证身份进行资源归属和权限判断，不信任客户端传入的用户身份。
- 公开、登录可见、指定用户可见和仅自己可见四级权限必须覆盖详情、列表、搜索、缓存、媒体、互动和统计场景。
- 禁止以简化实现为由绕过数据隔离、认证、授权、输入校验或敏感信息保护。
- 修改权限模型、数据隔离规则或删除策略前必须新增明确的决策记录。

## 开发与验证

- 变更应保持小而可验证，避免在一个任务中混入无关重构。
- 新增或修改行为时同步补充自动化测试和 `test-plan.md`。
- 完成任务前运行与变更相关的检查，并在 `tasks.md` 中记录结果。
- 不得提交密码、令牌、私钥、真实用户数据或本地环境配置。
- 新增依赖必须说明用途，优先选择维护活跃且确有必要的组件。

## 完成交付

- 确认需求、实现、测试和文档一致。
- 更新当前迭代的任务状态和必要的决策记录。
- 明确说明已完成内容、验证结果、遗留问题和下一步建议。

